interface:
  display_name: "Project Feedback Loop"
  short_description: "Route authorized project feedback to issues"
  default_prompt: "Use $project-feedback-loop for user-authorized project feedback targets or developer-mode GitHub issue maintenance."
policy:
  allow_implicit_invocation: true
